Purpose:
  A quick session to spin up a simple Spring Boot backend in Java.
  
About:
  Uses a rest controller to return HTTP responses when a user accesses each endpoint:
  
    /hello
    /bye
    
  Serves locally on port 8080. Endpoints can be reached at the following: 
     http://localhost:8080/hello
     http://localhost:8080/bye

